#include "bits/stdc++.h"
using namespace std;

typedef struct ListNode{
    int val;
    ListNode *next;
    ListNode(int v): val(v), next(NULL) {}
} ListNode;

ListNode* mergeList(vector<ListNode*> lists) {
    ListNode* res;
    int n = lists.size();
    for(int i = 0; i < n; i++) {
        
    }
}